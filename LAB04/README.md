# DL-04-RAG System Development I

Build a simple RAG retrieval pipeline from scratch using Python, including PDF loading, chunking, embeddings, a vector database, and semantic document search.
 
---- 
# Structure

```text
RAG-Project/
│
├── data/                                   # Stores the original documents
│   ├── diabetes_q_a.txt                    # Sample questions for testing
│   └── golden_set.json                     # Ground truth data for evaluation
│
├── evaluation/                             # Evaluation module for the RAG system
│   ├── __init__.py                         # Python package initialization
│   ├── build_golden_set.py                 # Script to build evaluation dataset
│   ├── eval_generation.py                  # Evaluates LLM generation quality
│   ├── eval_retrieval.py                   # Evaluates retrieval performance
│   └── metrics.py                          # Evaluation metrics definitions
│
├── labs/                                   # Step-by-step lab scripts
│   ├── lab01_extract_text.py               # Extract text from PDF
│   ├── lab02_chunking.py                   # Split text into chunks
│   ├── lab03_create_embeddings.py          # Generate embeddings
│   ├── lab04_create_vector_db.py           # Build the FAISS vector database
│   ├── lab05_query_embedding.py            # Create query embeddings
│   ├── lab06_similarity_search.py          # Retrieve Top-k relevant chunks
│   └── lab07_complete_retrieval.py         # Complete retrieval pipeline
│
├── outputs/                                # Stores outputs generated by each lab
│   ├── chunks.json                         # Text chunks with metadata
│   ├── embeddings.npy                      # Embedding vectors
│   ├── extracted_text.json                 # Extracted PDF text with page numbers
│   └── retrieval_results.json              # Top-k retrieval results
│
├── src/                                    # Reusable core modules for RAG pipeline
│   ├── __init__.py                         # Python package initialization
│   ├── document_loader.py                  # Document loading and text extraction
│   ├── embedding_model.py                  # Embedding generation model
│   ├── generator.py                        # LLM interaction and response generation
│   ├── hybrid_retriever.py                 # Hybrid (sparse + dense) retriever
│   ├── index_meta.py                       # Index metadata management
│   ├── memory.py                           # Conversation memory management
│   ├── prompt_templates.py                 # Prompt templates for LLM
│   ├── query_transform.py                  # Query transformation and rewriting
│   ├── rag_pipeline.py                     # Main RAG pipeline orchestrator
│   ├── rerankers.py                        # Document reranking models
│   ├── retriever.py                        # Semantic retrieval
│   ├── text_splitter.py                    # Text processing and chunking
│   └── vector_store.py                     # Vector database operations
│
├── vector_db/                              # Stores the vector database
│   ├── bm25_index.pkl                      # Sparse BM25 index for hybrid search
│   ├── chunk_store.json                    # Chunks and metadata
│   ├── document.index                      # Dense FAISS vector index
│   └── index_meta.json                     # Metadata for the indices
│
├── build_index.py                          # Script to build the vector index
├── config.py                               # Project configuration
├── create_pptx.py                          # Script to generate presentation
├── main.py                                 # Application entry point
├── RAG_System_Workflow.html                # RAG workflow diagram/visualization
├── RAG_System_Workflow.pptx                # RAG workflow presentation
└── requirements.txt                        # Required Python packages
```

# Project Components Overview

The main components of the RAG system are broken down as follows:

*   **`main.py`**: The application entry point that initializes and runs the RAG system.
*   **`src/rag_pipeline.py`**: The core orchestrator that connects retrieval and generation steps to form the complete RAG pipeline.
*   **`src/document_loader.py` & `src/text_splitter.py`**: Responsible for loading raw documents and processing/chunking them into manageable pieces for embedding.
*   **`src/embedding_model.py`**: Handles generating vector embeddings from text chunks.
*   **`src/vector_store.py`**: Manages the storage and querying of the dense vector database (e.g., FAISS).
*   **`src/retriever.py` & `src/hybrid_retriever.py`**: Execute similarity search to retrieve the most relevant chunks from the vector database based on the user query.
*   **`src/generator.py`**: Interacts with the LLM to generate the final answer using the retrieved context.
*   **`src/prompt_templates.py`**: Defines the templates used to structure the context and query for the LLM.
*   **`config.py`**: Centralizes all configuration settings like paths, model parameters, and thresholds.
*   **`data/` & `vector_db/`**: Directories that hold the raw input documents (`data`) and the generated indexing files used for fast retrieval (`vector_db`).

# Summary

RAG (Retrieval-Augmented Generation) คือเทคนิคที่ผสานการค้นหาข้อมูลจากแหล่งความรู้ภายนอกเข้ากับการสร้างคำตอบของโมเดล AI
ช่วยให้ AI ตอบคำถามได้แม่นยำ ทันสมัย และอ้างอิงข้อมูลจริงมากกว่าการใช้ความรู้ที่ฝึกมาเพียงอย่างเดียว
กระบวนการหลักประกอบด้วยการค้นคืนข้อมูล (Retrieval) แล้วส่งข้อมูลที่เกี่ยวข้องให้โมเดลสร้างคำตอบ (Generation)
RAG นิยมใช้ในแชตบอต ระบบถาม-ตอบ เอกสารองค์กร และระบบค้นหาความรู้ เพื่อเพิ่มความถูกต้องและลดการสร้างข้อมูลที่ไม่เป็นจริง (Hallucination)
